{
  "name": "products",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "pnpm -r build",
    
    "libs:build": "pnpm --filter './libs/*' build",

    "server:build": "pnpm --filter products-server build",
    "server:start": "pnpm --filter products-server start",
    "server:start:debug": "pnpm --filter products-server start:debug",
    "server:test": "pnpm --filter products-server test",
    "server:test:it": "pnpm --filter products-server test:it"
  }
}
